extends layouts/main
include mixins/videoBlock

block content
    .videos
        h1 video
        each item in Videos
           +videoBlock({
               id:item.id,
               title:item.title,
               views:item.views,
               videoFile:item.videoFile
           })